{
  "skills": [
    {
      "name": "list_all_skills",
      "description": "View the list of all available skills and tools in the current system",
      "file_name": "skills/basics.py",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "run_shell_command",
      "description": "Execute a shell command and return its output (stdout and stderr). Use this for system operations like git, ls, or checking service status.",
      "file_name": "skills/basics.py",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The shell command to execute"
          },
          "timeout": {
            "type": "integer",
            "description": "Maximum execution time in seconds (default: 30)",
            "default": 30
          }
        },
        "required": [
          "command"
        ]
      }
    },
    {
      "name": "launch_application",
      "description": "Launch or open specified applications by name or path.",
      "file_name": "skills/basics.py",
      "parameters": {
        "type": "object",
        "properties": {
          "application_name": {
            "type": "string",
            "description": "Name of the application to launch"
          },
          "application_path": {
            "type": "string",
            "description": "Full path to the application executable file"
          },
          "arguments": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of command line arguments"
          },
          "wait_for_completion": {
            "type": "boolean",
            "description": "Whether to wait for the application to exit",
            "default": false
          }
        },
        "required": []
      }
    },
    {
      "name": "get_current_weather",
      "description": "Fetch current weather data for a specified location.",
      "file_name": "skills/basics.py",
      "parameters": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "City name and optional country code"
          },
          "api_key": {
            "type": "string",
            "description": "API key"
          }
        },
        "required": [
          "location"
        ]
      }
    },
    {
      "name": "configure_autogen_deepseek",
      "description": "Configure AutoGen with DeepSeek model for multi-agent conversations. This function sets up an assistant agent and user proxy agent with DeepSeek API integration.",
      "file_name": "skills/generated/autogen_deepseek_config.py",
      "parameters": {
        "api_key": {
          "type": "string",
          "description": "DeepSeek API key",
          "required": true
        },
        "model_name": {
          "type": "string",
          "description": "DeepSeek model name",
          "default": "deepseek-chat",
          "required": false
        },
        "base_url": {
          "type": "string",
          "description": "API base URL",
          "default": "https://api.deepseek.com",
          "required": false
        },
        "temperature": {
          "type": "number",
          "description": "Sampling temperature (0.0 to 2.0)",
          "default": 0.7,
          "required": false
        },
        "max_tokens": {
          "type": "integer",
          "description": "Maximum tokens to generate",
          "required": false
        },
        "timeout": {
          "type": "integer",
          "description": "Request timeout in seconds",
          "default": 120,
          "required": false
        },
        "system_message": {
          "type": "string",
          "description": "System message for the assistant",
          "default": "You are a helpful AI assistant.",
          "required": false
        },
        "work_dir": {
          "type": "string",
          "description": "Working directory for file operations",
          "default": "./autogen_work",
          "required": false
        },
        "max_consecutive_auto_reply": {
          "type": "integer",
          "description": "Maximum consecutive auto-replies",
          "default": 10,
          "required": false
        },
        "human_input_mode": {
          "type": "string",
          "description": "Human input mode (ALWAYS, NEVER, TERMINATE)",
          "default": "NEVER",
          "required": false
        },
        "code_execution_config": {
          "type": "object",
          "description": "Code execution configuration",
          "required": false
        }
      }
    },
    {
      "name": "monitor_system_resources",
      "description": "Monitor system CPU, memory, disk, and network usage",
      "file_name": "skills/generated/resource_monitor.py",
      "parameters": {
        "type": "object",
        "properties": {
          "interval": {
            "type": "number",
            "description": "Time interval between measurements in seconds",
            "default": 1.0
          },
          "duration": {
            "type": "number",
            "description": "Total monitoring duration in seconds. If not provided, returns single measurement",
            "nullable": true
          },
          "include_disk": {
            "type": "boolean",
            "description": "Whether to include disk usage statistics",
            "default": true
          },
          "include_network": {
            "type": "boolean",
            "description": "Whether to include network statistics",
            "default": true
          }
        },
        "required": []
      }
    }
  ]
}